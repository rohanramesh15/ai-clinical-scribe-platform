# Lower-bound pins so pip resolves a mutually-compatible set (google-genai pulls
# a recent pydantic; exact-pinning pydantic caused a ResolutionImpossible).
# ---- Web / async ----
fastapi>=0.115
uvicorn[standard]>=0.32
gunicorn>=23
python-multipart>=0.0.18
sse-starlette>=2.1

# ---- DB ----
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
pgvector>=0.3.6
alembic>=1.14

# ---- Config / secrets ----
pydantic>=2.11
pydantic-settings>=2.7
email-validator>=2.2
boto3>=1.35

# ---- AI ----
google-genai>=2.9

# ---- Auth ----
argon2-cffi>=23.1

# ---- Embeddings (ICD-10 vector search; seed + query time) ----
sentence-transformers>=3.3
