# ---- Web / async ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
python-multipart==0.0.20
sse-starlette==2.2.1

# ---- DB ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0

# ---- Config / secrets ----
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0
boto3==1.35.90

# ---- AI ----
google-genai==1.2.0

# ---- Auth ----
argon2-cffi==23.1.0

# ---- Embeddings (ICD-10 vector search; used at seed time AND at query time) ----
# CPU torch is pulled transitively; pin sentence-transformers, let it resolve torch.
sentence-transformers==3.3.1
